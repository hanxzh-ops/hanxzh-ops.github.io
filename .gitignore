.DS_Store
Thumbs.db

# Jekyll build output
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/
.bundle/
vendor/

# Camera / CAD masters — keep these OUTSIDE the repo (see MEDIA.md).
# Web derivatives are committed instead; originals live in cloud storage.
*.MOV
*.mov
*.HEIC
*.heic
*.CR2
*.NEF
*.dng
*.tif
*.tiff

# Large source exports that must be optimized before committing
assets/**/_originals/
assets/**/_raw/
